<%= form_with(model: people_in_team) do |form| %>
  <% if people_in_team.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(people_in_team.errors.count, "error") %> prohibited this people_in_team from being saved:</h2>

      <ul>
        <% people_in_team.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
