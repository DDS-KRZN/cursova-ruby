<p style="color: green"><%= notice %></p>

<h1>Команди</h1>
<table>
  <thead>
  <tr>
    <th>Назва</th>
    <th>Відділ</th>
    <th>Директор</th>
    <th>Дії</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>

    <% @teams.each do |t| %>
        <tr>
          <td><%= t.name %> </td>
          <td><%= t.department.name%> </td>
          <td><%= t.director.name %> </td>
          <td><%= link_to 'Перейти на сторінку', team_path(t.id) %></td>

        </tr>
      <% end %>
      </tbody>
</table>

  </div>

    </p>
<div>
<%= link_to "Створити команду", new_team_path %>
</div>